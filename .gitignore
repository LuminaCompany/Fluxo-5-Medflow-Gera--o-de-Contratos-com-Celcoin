# ───── Segredos / credenciais — NUNCA commitar ─────
# Variáveis de ambiente
.env
.env.*
*.env
!.env.example

# Config local de MCP (contém N8N_API_KEY)
.mcp.json
!.mcp.json.example

# Settings locais do Claude Code
.claude/settings.local.json

# Chaves e certificados
*.key
*.pem
*.p12
*.pfx
id_rsa
id_rsa.*

# Arquivos de credenciais comuns
credentials.json
*credentials*.json
secrets.json
*.secret
service-account*.json

# ───── Ruído de SO / editor ─────
.DS_Store
Thumbs.db
*.log
node_modules/
